auto-merge installed successfully.

Service DNS:
  http://{{ include "auto-merge.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local

{{- if .Values.ingress.enabled }}
Ingress hosts:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}
{{- end }}
{{- else }}
Ingress is disabled. To expose the webhook endpoint externally, set:
  --set ingress.enabled=true --set ingress.hosts[0].host=your.domain.example
The host MUST match the Webhook URL configured in your GitHub App settings.
{{- end }}

Diagnostics endpoint (requires DIAGNOSE_TOKEN in your Secret):
  GET /diagnose  — returns config + health summary (redacted)

Troubleshooting:
  kubectl logs -l app.kubernetes.io/instance={{ .Release.Name }} --tail=50
  kubectl exec -it deploy/{{ include "auto-merge.fullname" . }} -- wget -qO- http://localhost:3000/healthz

REMINDER: existingSecretName is set to "{{ .Values.existingSecretName }}".
The referenced Secret must exist before pods can start. Required keys:
  APP_ID, WEBHOOK_SECRET, and exactly one of PRIVATE_KEY / PRIVATE_KEY_PATH.
