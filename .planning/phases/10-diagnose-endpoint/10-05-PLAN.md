---
phase: 10-diagnose-endpoint
plan: 05
type: execute
wave: 3
depends_on: [10-04]
files_modified:
  - test/integration/diagnose-endpoint.test.ts
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04]
must_haves:
  truths:
    - "End-to-end test boots buildServer with real wiring (server.ts + diagnose handler) backed by msw GitHub mocks"
    - "SC1 covered: valid bearer + happy-path msw fixtures ⇒ 200 JSON with all 6 sections; Accept: text/markdown ⇒ 200 text/markdown"
    - "SC2 covered: no bearer ⇒ 401; wrong bearer ⇒ 401; no DIAGNOSE_TOKEN env ⇒ 503"
    - "SC3 covered: 11th burst hit from same client ⇒ 429 with Retry-After"
    - "SC4 covered: planted SLACK_WEBHOOK_URL / TELEGRAM_BOT_TOKEN tokens never appear in response body (string search)"
    - "Existing 614-test suite still green"
  artifacts:
    - path: test/integration/diagnose-endpoint.test.ts
      provides: "msw + buildServer end-to-end coverage of DIAG-01..04 SCs"
  key_links: []
---

<objective>
End-to-end integration test that wires `buildServer` (the real one from src/server.ts) against an msw-mocked GitHub API and asserts the 4 ROADMAP Success Criteria for Phase 10.

Purpose: Plans 10-01..10-04 cover their slices via unit/handler tests; this plan proves the seams hold together. Mirrors `test/integration/onboarding-bulk-install.test.ts` and `test/integration/setup-flow.test.ts` patterns — msw fixtures + `app.inject` (no real listen).
Output: One integration test file + green CI run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-diagnose-endpoint/10-CONTEXT.md
@.planning/phases/10-diagnose-endpoint/10-04-SUMMARY.md

<interfaces>
From src/server.ts (post Plan 10-04):
- `buildServer(deps: BuildServerDeps): Promise<FastifyInstance>` — call with the full deps payload; only `healthChecker`, `getInstallationOctokit`, `getAppOctokit`, `env`, `log` are needed for the diagnose path (webhook/setup blocks gated false).

From src/diagnose/types.ts:
- `DiagnoseReport`, `DiagnoseChecks` shape (D-10) — assertion target.

Test infra reference:
- `test/integration/onboarding-bulk-install.test.ts` — msw server lifecycle, signed webhook helpers, Fastify `app.inject`
- `test/integration/setup-flow.test.ts` — env stubbing pattern, in-process route exercise
- `test/integration/readyz-notify-status.test.ts` — healthChecker stubbing
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test/integration/diagnose-endpoint.test.ts covering DIAG-01..04 SCs</name>
  <read_first>
    - test/integration/onboarding-bulk-install.test.ts (msw setup style, fixture authoring)
    - test/integration/setup-flow.test.ts (env stubbing + buildServer integration pattern)
    - test/integration/readyz-notify-status.test.ts (healthChecker fake)
    - src/server.ts (post Plan 10-04 — confirm BuildServerDeps shape)
    - src/diagnose/handler.ts (route path, response shape)
    - .planning/phases/10-diagnose-endpoint/10-CONTEXT.md D-10, D-11, D-14 (response invariants)
  </read_first>
  <behavior>
    - **Scenario 1 (SC1 — full JSON):** msw stubs return success for all GitHub endpoints listed in Plan 10-02 interfaces; bearer valid; Accept omitted ⇒ status 200, content-type `application/json`, body keys exactly `['ok','owner','repo','checked_at','checks']`, `checks` keys exactly `['app_installed','app_permissions','config','branches','notify','onboarding']`
    - **Scenario 2 (SC1 — markdown):** same fixtures + `Accept: text/markdown` ⇒ status 200, content-type starts with `text/markdown`, body starts with `# Diagnose: testowner/testrepo`
    - **Scenario 3 (SC2 — no token env):** env without `DIAGNOSE_TOKEN` ⇒ status 503, body `{ error: 'diagnose-disabled' }`; even with valid Authorization header still 503
    - **Scenario 4 (SC2 — missing bearer):** env with DIAGNOSE_TOKEN, no Authorization header ⇒ status 401
    - **Scenario 5 (SC2 — wrong bearer, equal length):** wrong token of equal length ⇒ status 401
    - **Scenario 6 (SC2 — wrong bearer, different length):** wrong token of different length ⇒ status 401 (proves length-mismatch branch executes safely)
    - **Scenario 7 (SC3 — rate-limit):** 10 sequential hits with valid bearer + valid msw stubs ⇒ all 200; 11th hit ⇒ 429 with numeric `Retry-After` header > 0
    - **Scenario 8 (SC4 — redaction):** env has `SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T1/B1/SECRET_TOKEN_SEGMENT_XYZ` and `TELEGRAM_BOT_TOKEN=1234567890:AAA_SECRET_BOT_TOKEN_VALUE_AAAAAAAAAA`; valid bearer; happy path ⇒ 200; `JSON.stringify(body)` does NOT contain `'SECRET_TOKEN_SEGMENT_XYZ'` and does NOT contain `'AAA_SECRET_BOT_TOKEN_VALUE'`
    - **Scenario 9 (regression — app not installed):** msw returns 404 on `GET /repos/{o}/{r}/installation` ⇒ 200 with `body.checks.app_installed.status === 'error'`, other checks have `status: 'n/a'` (SC1 invariant: full key-set even on early-exit)
    - All scenarios run via `app.inject({ method: 'GET', url: '/diagnose/testowner/testrepo', headers: {...} })` — no real listen
  </behavior>
  <action>
    Create `test/integration/diagnose-endpoint.test.ts` modeled after `test/integration/onboarding-bulk-install.test.ts`:

    1. **Top-of-file**: `import { setupServer } from 'msw/node'; import { http, HttpResponse } from 'msw';` + `buildServer` from `../../src/server.js` + types.
    2. **msw lifecycle**: `beforeAll(() => server.listen({ onUnhandledRequest: 'error' }))`, `afterEach(() => server.resetHandlers())`, `afterAll(() => server.close())`.
    3. **Fixture builder** `buildHappyMsw()`: returns the array of msw handlers stubbing every endpoint listed in Plan 10-02 interfaces with success payloads (`installation_id: 999`, permissions matching REQUIRED_PERMISSIONS exactly, default_branch `main`, branches main/dev existing + unprotected, config file present with valid YAML, no open onboarding PR).
    4. **Helper `boot(envOverrides)`**: stubs env (`DIAGNOSE_TOKEN`, `APP_ID`, `PRIVATE_KEY`, `WEBHOOK_SECRET`, optional `SLACK_WEBHOOK_URL`, `TELEGRAM_BOT_TOKEN`, `NOTIFY_HEALTHCHECK_TTL_MS: 900000`), constructs a fake `healthChecker` returning `{ slack: 'ok', telegram: 'ok' }`, returns `buildServer({ env, log: pino({ level: 'silent' }), healthChecker, getAppOctokit: () => realAppOctokit, getInstallationOctokit: async () => realInstallationOctokit })`. The two Octokit instances are constructed with a base URL pointed at msw's interception (default `https://api.github.com` works — msw intercepts that host).

       Note: `getAppOctokit` and `getInstallationOctokit` from `src/auth.ts` require `createProbot(env)` to have been called first to set `bootEnv`. The test must either (a) call `createProbot(env)` in `boot()` to initialise auth state, OR (b) inject test-local Octokit instances directly (preferred — keeps the test from depending on real JWT signing). Choose (b): construct `new Octokit()` (no auth) since msw intercepts at the HTTP layer and doesn't verify the Authorization header. Document this with a `// WHY` comment in the test.

    5. **Implement each scenario** as a separate `it(...)` block. For Scenario 7 (rate-limit), the test relies on the per-server-instance rate-limit singleton inside handler.ts; running 11 `app.inject` calls against the SAME `app` exhausts the budget. Each test gets a fresh `app` via `beforeEach` so scenarios don't contaminate each other.
    6. **Assertions** use `expect(response.statusCode).toBe(...)`, `expect(response.headers['content-type']).toMatch(...)`, `expect(JSON.parse(response.body))...`. For Scenario 8, use `expect(response.body).not.toContain('SECRET_TOKEN_SEGMENT_XYZ')` — string search on raw body is the most resilient redaction assertion.

    English comments. No `any`. No real network (msw `onUnhandledRequest: 'error'` enforces this).
  </action>
  <verify>
    <automated>npx vitest run test/integration/diagnose-endpoint.test.ts && npx vitest run</automated>
  </verify>
  <done>New integration test green; full suite (614+) still green; biome clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness ↔ buildServer | In-process; trust is total. No external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-16 | (test-only) | integration test fixtures | accept | Tests don't reach production; no threat surface introduced |
| T-10-17 | Information Disclosure | regression — redaction assertion strength | mitigate | Scenario 8 uses string-contains on raw body so any future refactor that accidentally inlines a token segment fails fast |
</threat_model>

<verification>
- `npx vitest run test/integration/diagnose-endpoint.test.ts` — all 9 scenarios pass
- `npx vitest run` — full suite green, count >= prior 614
- `npx biome check test/integration/diagnose-endpoint.test.ts` — exit 0
</verification>

<success_criteria>
- Every Phase 10 ROADMAP Success Criterion (1-4) has at least one scenario asserting it
- DIAG-01, DIAG-02, DIAG-03, DIAG-04 each covered end-to-end
- No FROZEN component touched; only test file added
</success_criteria>

<output>
Create `.planning/phases/10-diagnose-endpoint/10-05-SUMMARY.md` when done.
</output>
