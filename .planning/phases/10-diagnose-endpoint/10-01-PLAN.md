---
phase: 10-diagnose-endpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/diagnose/rateLimit.ts
  - src/diagnose/rateLimit.test.ts
  - src/diagnose/redact.ts
  - src/diagnose/redact.test.ts
autonomous: true
requirements: [DIAG-03, DIAG-04]
must_haves:
  truths:
    - "Rate-limiter allows 10 hits per IP per 60s rolling window and 429s the 11th"
    - "Rate-limiter Retry-After header equals remaining TTL in seconds"
    - "Slack webhook URLs are reduced to host+path-prefix + '****' before leaving the process"
    - "Secret helper exposes only { present, byte_length } — never raw value"
  artifacts:
    - path: src/diagnose/rateLimit.ts
      provides: "createDiagnoseRateLimit() factory returning { check(ip): { allowed: boolean, retryAfterSec?: number } }"
    - path: src/diagnose/redact.ts
      provides: "redactSlackUrl(url?) and redactSecret(value?) pure helpers"
    - path: src/diagnose/rateLimit.test.ts
      provides: "Unit coverage of allow/deny/expiry/retry-after"
    - path: src/diagnose/redact.test.ts
      provides: "Unit coverage of nullish, normal URL, no-slash URL, undefined secret"
  key_links: []
---

<objective>
Provide the two pure-logic primitives consumed by the diagnose handler: an LRU TTL rate-limit (DIAG-03) and a redaction helper (DIAG-04). No Fastify, no Octokit, no I/O — just deterministic functions plus their unit tests.

Purpose: Isolate the security-critical primitives so they can be reasoned about and tested without lifting an HTTP server. Mirrors the `src/cascade/sourceShaDedup.ts` LRU idiom and the v1.0 "direct-impl over libs" stance.
Output: Two source modules + two test files committed; ready to import from Plan 10-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-diagnose-endpoint/10-CONTEXT.md

<interfaces>
From src/cascade/sourceShaDedup.ts (LRUCache idiom — same package, same pattern):
- `new LRUCache<KEY, VAL>({ max, ttl, ttlAutopurge: true, ttlResolution: 0, perf: { now: () => Date.now() } })`
- `cache.get(key)`, `cache.set(key, val)`, `cache.has(key)`
- `lru-cache` is already in dependencies — do NOT add a new dep.

From src/env.ts:
- `DIAGNOSE_TOKEN: z.string().min(16).optional()` — consumed only in Plan 10-04; not relevant here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement src/diagnose/rateLimit.ts with LRUCache and unit tests</name>
  <read_first>
    - src/cascade/sourceShaDedup.ts (LRUCache idiom — match it)
    - .planning/phases/10-diagnose-endpoint/10-CONTEXT.md D-08 (rate-limit spec)
  </read_first>
  <behavior>
    - Test 1: first 10 calls with same ip → `{ allowed: true }`
    - Test 2: 11th call within 60s → `{ allowed: false, retryAfterSec: <int in [1..60]> }`
    - Test 3: after advancing clock past `resetAt` (vitest fake timers) → counter resets, allowed again
    - Test 4: two different IPs do not contend (each gets own 10-budget)
    - Test 5: `retryAfterSec` is rounded up (Math.ceil) so 0.4s remaining → 1
  </behavior>
  <action>
    Create `src/diagnose/rateLimit.ts` exporting `createDiagnoseRateLimit(opts?: { max?: number; windowMs?: number }): { check(ip: string): { allowed: boolean; retryAfterSec?: number } }`. Defaults: `max=10_000` LRU entries, `windowMs=60_000`. Store `LRUCache<string, { count: number; resetAt: number }>` with `ttl: windowMs`, `ttlAutopurge: true`, `ttlResolution: 0`. `check(ip)` logic per D-08: lookup entry; if missing or `Date.now() >= entry.resetAt` → set `{ count: 1, resetAt: Date.now() + windowMs }` and return `{ allowed: true }`; else if `entry.count >= 10` → return `{ allowed: false, retryAfterSec: Math.ceil((entry.resetAt - Date.now()) / 1000) }`; else mutate `entry.count++` and return `{ allowed: true }`. The cap (10) is a hard constant per DIAG-03 — do NOT make it configurable. Comments in English explaining WHY LRU bound (memory cap against IP-space DoS) and WHY `ttlResolution: 0` (live clock for fake-timer tests). Co-located test file `src/diagnose/rateLimit.test.ts` uses `vi.useFakeTimers()` to exercise window rollover deterministically.
  </action>
  <verify>
    <automated>npx vitest run src/diagnose/rateLimit.test.ts</automated>
  </verify>
  <done>Test file exits 0; module exports the factory; no `any`; no new dependencies in package.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/diagnose/redact.ts and unit tests</name>
  <read_first>
    - .planning/phases/10-diagnose-endpoint/10-CONTEXT.md D-16, D-17 (redaction spec)
  </read_first>
  <behavior>
    - `redactSlackUrl(undefined)` → `null`
    - `redactSlackUrl("https://hooks.slack.com/services/T1/B1/abc123")` → `"https://hooks.slack.com/services/T1/B1/****"`
    - `redactSlackUrl("https://hooks.slack.com/services")` (no trailing path segment) → still ends in `/****` (last `/` index used)
    - `redactSecret(undefined)` → `{ present: false, byte_length: 0 }`
    - `redactSecret("abc")` → `{ present: true, byte_length: 3 }` (use `Buffer.byteLength`, not `.length`, to count UTF-8 bytes correctly)
  </behavior>
  <action>
    Create `src/diagnose/redact.ts` exporting two pure functions per D-16: `redactSlackUrl(url: string | undefined): string | null` — if undefined return null; otherwise `url.slice(0, url.lastIndexOf('/') + 1) + '****'`. And `redactSecret(value: string | undefined): { present: boolean; byte_length: number }` — return `{ present: false, byte_length: 0 }` for undefined/empty, else `{ present: true, byte_length: Buffer.byteLength(value, 'utf8') }`. English comment on each: WHY suffix-mask (preserve workspace/channel id for operator recognition, hide token segment) and WHY byte_length (operator can detect truncation without seeing secret). No regex, no validation — pure transforms. Co-located `src/diagnose/redact.test.ts` covers the five behavior cases above.
  </action>
  <verify>
    <automated>npx vitest run src/diagnose/redact.test.ts</automated>
  </verify>
  <done>Tests pass; both functions exported; no `any`; module is tree-shakeable (pure, no side effects at top level).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public HTTP → diagnose handler | Untrusted internet client hits `GET /diagnose/...`; rate-limit is the first guard against probing/DoS |
| Internal state → JSON response | Env-resident secrets (Slack URL, Telegram token, PRIVATE_KEY) must never serialize to wire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Denial of Service | `src/diagnose/rateLimit.ts` (per-IP counter) | mitigate | LRU `max=10_000` bounds attacker-controlled key cardinality; 10/min cap per D-08 enforced via `check(ip)` |
| T-10-02 | Information Disclosure | `src/diagnose/redact.ts` (Slack URL) | mitigate | `redactSlackUrl` masks segment after last `/`; D-16 spec |
| T-10-03 | Information Disclosure | `src/diagnose/redact.ts` (secret length leak) | accept | byte_length is exposed deliberately for operator truncation detection (D-16); secret value itself never returned |
| T-10-04 | Tampering | rate-limit window bypass via clock manipulation | accept | Server clock is operator-controlled; in-process LRU has no external dependency to tamper |
</threat_model>

<verification>
- `npx vitest run src/diagnose/` exits 0
- `npx biome check src/diagnose/rateLimit.ts src/diagnose/redact.ts` exits 0
- No new entries in package.json `dependencies`
</verification>

<success_criteria>
- Both source files exist with documented public APIs
- Both test files exist and pass deterministically (no real timers)
- Mappable to DIAG-03 (rate-limit) and DIAG-04 (redaction) acceptance facts
</success_criteria>

<output>
Create `.planning/phases/10-diagnose-endpoint/10-01-SUMMARY.md` when done.
</output>
